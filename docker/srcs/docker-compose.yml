networks:
  webnet:
    name: webnet
    driver: bridge

services:
  website:
    container_name: website
    image: website
    build: ./requirements/website
    ports:
      - 5000:5000
    networks:
      - webnet
    restart: unless-stopped
